FROM node:22-alpine
RUN corepack enable

WORKDIR /app

COPY package.json pnpm-workspace.yaml ./
COPY packages/web/package.json packages/web/

RUN pnpm install --filter @mealodic/web

CMD ["pnpm", "--filter", "@mealodic/web", "dev"]
